# FastWAM Tiny Demo Reproduction Change Log

Date: 2026-05-28 06:29:22 MDT
Machine: trinity
GPU: unavailable in current session; nvidia-smi cannot communicate with the NVIDIA driver
Python: /home/ra-ugrad/anaconda3/bin/python 3.12.7
Torch: 2.6.0+cu124
CUDA: torch.version.cuda=12.4, torch.cuda.is_available()=False, torch.cuda.device_count()=0

## Goal

This change does not attempt full Fast-WAM paper reproduction. The goal is a
student-runnable tiny reproduction path that proves:

- the repo can import in the current environment,
- a FastWAM-like batch contract can be constructed without LIBERO/RoboTwin,
- forward/loss/backward/optimizer/checkpoint can run through the existing trainer,
- GPU/profile diagnostics are recorded even when CUDA is unavailable.

## Project-internal changes

- Added file: `src/fastwam/datasets/demo_synthetic.py`
  Reason: provide an in-memory synthetic dataset instead of downloading LIBERO/RoboTwin.
  Summary: emits `video`, `action`, `proprio`, `prompt`, `context`, `context_mask`, padding masks, and metadata. Shapes mimic the real trainer path: video `[3,T,H,W]`, action `[A,D]`, proprio `[A,P]`, context `[L,E]`.
  How to run: used by `task=demo_tiny_fastwam`.
  Verification result: smoke test loaded a batch with video `(8,3,5,64,64)`, action `(8,8,7)`, proprio `(8,8,8)`, context `(8,16,64)`.

- Added file: `src/fastwam/models/demo_tiny_fastwam.py`
  Reason: avoid loading Wan2.2/T5/ActionDiT while preserving the trainer-facing model API.
  Summary: implements `DemoTinyFastWAM` with `.dit`, `training_loss()`, `save_checkpoint()`, and `load_checkpoint()`. Supported modes are `action_only`, `joint_video_action`, and `idm_proxy`.
  How to run: selected by `configs/model/demo_tiny_fastwam.yaml`.
  Verification result: action-only, joint, and IDM smoke tests all completed loss/backward/optimizer step without NaN.

- Added file: `configs/data/demo_synthetic.yaml`
  Reason: Hydra data config for the tiny synthetic dataset.
  Summary: default length 64, 5 video frames, 64x64 image size, action horizon 8, action dim 7, proprio dim 8, context dim 64.
  How to run: `python scripts/train.py task=demo_tiny_fastwam`.
  Verification result: instantiated by smoke test and tiny train.

- Added file: `configs/model/demo_tiny_fastwam.yaml`
  Reason: Hydra model config for the tiny proxy model.
  Summary: hidden dim 128, action-only default, optional video/latent auxiliary losses.
  How to run: override `model.mode=joint_video_action` or use the task configs below.
  Verification result: action-only tiny train reached 60 steps.

- Added file: `configs/task/demo_tiny_fastwam.yaml`
  Reason: main tiny action-only baseline task.
  Summary: batch size 8, max steps 60, constant LR 3e-3, eval disabled, checkpoint saved at the end.
  How to run: `bash scripts/run_tiny_train.sh` or `python scripts/train.py task=demo_tiny_fastwam`.
  Verification result: loss decreased from 0.0697 at step 10 to 0.0140 at step 60.

- Added file: `configs/task/demo_tiny_joint.yaml`
  Reason: proxy experiment for joint action + future-video auxiliary training.
  Summary: inherits the tiny task and sets `model.mode=joint_video_action`, `model.lambda_video=0.1`.
  How to run: `python scripts/smoke_test_tiny_fastwam.py task=demo_tiny_joint`.
  Verification result: smoke loss 0.390614, including `loss_action=0.329885` and `loss_video=0.060730`.

- Added file: `configs/task/demo_tiny_idm.yaml`
  Reason: proxy experiment for imagine-then-act / IDM-style tiny training.
  Summary: inherits the tiny task and sets `model.mode=idm_proxy`, `model.lambda_latent=0.1`.
  How to run: `python scripts/smoke_test_tiny_fastwam.py task=demo_tiny_idm`.
  Verification result: smoke loss 0.362365, including `loss_action=0.317393` and `loss_latent=0.044971`.

- Added file: `scripts/smoke_test_tiny_fastwam.py`
  Reason: fast single-batch validation script.
  Summary: loads Hydra config, builds dataset/model/dataloader, runs forward/loss/backward/optimizer step, prints shapes and memory status.
  How to run: `python scripts/smoke_test_tiny_fastwam.py`.
  Verification result: `SMOKE TEST OK`; loss 0.325540; no NaN.

- Added file: `scripts/run_tiny_train.sh`
  Reason: reproducible tiny train wrapper with log and GPU profile capture.
  Summary: runs `scripts/train.py task=demo_tiny_fastwam`, saves log to `runs/demo_tiny_fastwam/tiny_train_log.txt`, starts `nvidia-smi` CSV profiling when available.
  How to run: `bash scripts/run_tiny_train.sh`.
  Verification result: 60-step train completed and wrote checkpoint under `runs/demo_tiny_fastwam/tiny_train/checkpoints/`.

- Added file: `scripts/benchmark_tiny_dataloader.py`
  Reason: benchmark batch size / worker / pin-memory settings for the tiny path.
  Summary: measures step time and writes `runs/demo_tiny_fastwam/dataloader_benchmark.csv`. It now skips `num_workers>0` safely when multiprocessing sockets are unavailable in the current sandbox/session.
  How to run: `python scripts/benchmark_tiny_dataloader.py`.
  Verification result: benchmark completed; worker settings skipped with `PermissionError(1, 'Operation not permitted')`.

- Modified file: `src/fastwam/trainer.py`
  Reason: make existing trainer robust for CPU/non-Deepspeed tiny runs and expose dataloader/profile knobs.
  Summary: handles missing Deepspeed plugin, supports optional `pin_memory` and `persistent_workers`, logs GPU memory when CUDA is available, reports unavailable GPU otherwise, and removes a grad-norm tensor warning.
  How to run: used automatically by `scripts/train.py`.
  Verification result: official trainer ran 60 tiny steps on CPU and saved final checkpoint.

- Modified file: `.gitignore`
  Reason: the repo ignored all `*.txt`, which hid the required `changes-log.txt`.
  Summary: added a precise `!changes-log.txt` exception while keeping run logs and other text outputs ignored.
  How to run: no runtime command needed.
  Verification result: `git status --short` now reports `changes-log.txt`.

- Added file: `RUN_TINY_DEMO.md`
  Reason: user-facing runbook for the tiny reproduction path.
  Summary: explains what the demo does, how it differs from full paper reproduction, installation, smoke test, tiny train, GPU profile, and next experiments.
  How to run: read and follow commands in the file.
  Verification result: commands in the runbook were executed during this session unless marked as future extension.

## System / environment changes

- Command attempted inside sandbox:
  `python -m pip install -e . --no-deps`
  Result: failed because isolated build dependency resolution tried to reach PyPI for `setuptools>=68` and network was restricted.

- Command executed with escalation:
  `python -m pip install -e . --no-deps`
  Reason: install this repo as editable in the current Python without installing full heavyweight dependencies.
  Result: installed `fastwam-0.1.0` editable from `/home/ra-ugrad/Ajax/FastWAM-fork`.
  External effect: wrote package metadata to the active Python environment under `/home/ra-ugrad/anaconda3/lib/python3.12/site-packages`.

- Command executed with escalation:
  `python -m pip install hydra-core==1.3.2 accelerate==1.12.0`
  Reason: these were missing and are required by `scripts/train.py`, `fastwam.runtime`, and `Wan22Trainer`.
  Result: installed `hydra-core 1.3.2` and `accelerate 1.12.0`.
  External effect: downloaded small Python wheels from PyPI and installed into the active Python environment.
  Note: full project dependencies remain incomplete by design; missing heavy deps include `deepspeed`, `datasets`, `av`, `torchcodec`, `wandb`, and others. The tiny demo does not require them.

- Command executed with escalation:
  `python -m pip install termcolor==2.5.0`
  Reason: `fastwam.utils.misc` imports `termcolor`, and `fastwam.runtime` failed without it.
  Result: installed `termcolor 2.5.0`.
  External effect: downloaded and installed one small Python wheel into the active Python environment.

- Command executed with escalation:
  `pkill -f benchmark_tiny_dataloader.py`
  Reason: stop a stuck benchmark process after `num_workers>0` hit multiprocessing socket permission errors.
  Result: stopped the stuck benchmark process.

- File permission change:
  `chmod +x scripts/smoke_test_tiny_fastwam.py scripts/run_tiny_train.sh scripts/benchmark_tiny_dataloader.py`
  Reason: make demo scripts directly executable.
  Scope: project-internal file mode only.

- No conda environment was created.
- No sudo was used.
- No CUDA, driver, shell startup file, or system service was changed.
- No LIBERO/RoboTwin dataset or checkpoint was downloaded.
- No permanent environment variables were set.

Important environment note:

- `python` points to `/home/ra-ugrad/anaconda3/bin/python` with Python 3.12.7.
- bare `pip` points to `/home/ra-ugrad/.local/bin/pip` for Python 3.10.
- Use `python -m pip`, not bare `pip`, in this environment.

## Commands executed

Key commands, in approximate order:

```bash
pwd
ls
git status --short
find . -maxdepth 3 -type f | sed 's#^./##' | sort | head -200
sed -n '1,240p' README.md
sed -n '1,240p' README_zh.md
sed -n '1,220p' pyproject.toml
sed -n '1,260p' scripts/train.py
sed -n '1,980p' src/fastwam/trainer.py
sed -n '1,620p' src/fastwam/runtime.py
sed -n '1,260p' configs/train.yaml
sed -n '1,260p' configs/task/libero_uncond_2cam224_1e-4.yaml
sed -n '1,260p' configs/data/libero_2cam.yaml
sed -n '1,620p' src/fastwam/models/wan22/fastwam.py
sed -n '1,620p' src/fastwam/models/wan22/action_dit.py
which python
python --version
which pip
pip --version
nvidia-smi
python - <<'PY'
import torch
print("torch:", torch.__version__)
print("cuda available:", torch.cuda.is_available())
print("cuda version:", torch.version.cuda)
print("gpu count:", torch.cuda.device_count())
PY
python -m pip install -e . --no-deps
python -m pip install hydra-core==1.3.2 accelerate==1.12.0
python -m pip install termcolor==2.5.0
python scripts/smoke_test_tiny_fastwam.py
bash scripts/run_tiny_train.sh
python scripts/benchmark_tiny_dataloader.py
python scripts/smoke_test_tiny_fastwam.py task=demo_tiny_joint output_dir=./runs/demo_tiny_fastwam/smoke_joint
python scripts/smoke_test_tiny_fastwam.py task=demo_tiny_idm output_dir=./runs/demo_tiny_fastwam/smoke_idm
```

## Tiny demo results

- smoke test:
  - command: `python scripts/smoke_test_tiny_fastwam.py`
  - result: `SMOKE TEST OK`
  - shapes:
    - video: `(8, 3, 5, 64, 64)`
    - action: `(8, 8, 7)`
    - proprio: `(8, 8, 8)`
    - context: `(8, 16, 64)`
  - loss: `0.325540`
  - grad norm: `0.157021`

- tiny train:
  - command: `bash scripts/run_tiny_train.sh`
  - max steps: 60
  - batch size: 8
  - device: CPU, because CUDA is unavailable
  - step 10 loss: `0.0697`
  - step 20 loss: `0.0545`
  - step 30 loss: `0.0330`
  - step 40 loss: `0.0181`
  - step 50 loss: `0.0109`
  - final step 60 loss: `0.0140`
  - speed at final log: `250.57 step/s`, `2004.56 samples/s`
  - checkpoint: `runs/demo_tiny_fastwam/tiny_train/checkpoints/weights/step_000060.pt`

- GPU memory:
  - unavailable; `torch.cuda.is_available()` is False.

- approximate GPU utilization:
  - unavailable; `nvidia-smi` failed with driver communication error.

- log path:
  - `runs/demo_tiny_fastwam/tiny_train_log.txt`
  - `runs/demo_tiny_fastwam/gpu_profile.csv`
  - `runs/demo_tiny_fastwam/dataloader_benchmark.csv`

## GPU utilization diagnosis

- observed issue:
  - `nvidia-smi` fails: `NVIDIA-SMI has failed because it couldn't communicate with the NVIDIA driver.`
  - PyTorch warns: `Can't initialize NVML`.
  - `torch.cuda.is_available()` is False and GPU count is 0.

- likely bottleneck:
  - primary blocker is not model size or dataloader speed; the current session has no usable CUDA device/driver path.
  - under this sandbox/session, DataLoader multiprocessing workers also cannot start because local socket creation is blocked: `PermissionError(1, 'Operation not permitted')`.

- attempted fix:
  - tiny train defaults to CPU-safe `num_workers=0`, `pin_memory=false`, `persistent_workers=false`.
  - trainer now logs `gpu_mem=unavailable` instead of assuming CUDA.
  - benchmark script skips worker settings when multiprocessing is unavailable instead of hanging.

- result:
  - CPU tiny train works.
  - GPU utilization cannot be measured in the current session.
  - DataLoader benchmark results:

```text
setting                                           step_time   gpu_util_approx   memory_used   notes
bs=1, workers=0, pin=False, persistent=False      0.0021s     NA                NA            CUDA unavailable
bs=2, workers=0, pin=False, persistent=False      0.0025s     NA                NA            CUDA unavailable
bs=4, workers=0, pin=False, persistent=False      0.0026s     NA                NA            CUDA unavailable
bs=4, workers=2, pin=False, persistent=False      SKIP        NA                NA            multiprocessing unavailable
bs=4, workers=2, pin=True, persistent=True        SKIP        NA                NA            multiprocessing unavailable
```

## Remaining problems

- Current Python is 3.12.7, while official README recommends Python 3.10.
- Current Torch is 2.6.0+cu124, while `pyproject.toml` pins 2.7.1+cu128.
- Full FastWAM dependencies are not installed; this is intentional for the tiny demo but not enough for full LIBERO/RoboTwin reproduction.
- CUDA is not usable in the current session, so GPU memory/utilization cannot be validated.
- DataLoader `num_workers>0` is blocked in the current sandbox/session.
- The demo model is a proxy model, not Wan2.2/FastWAM. It validates the training interface and experiment scaffolding, not paper-scale model behavior.

## Next recommended experiments

- Run `demo_tiny_fastwam` on a session where `nvidia-smi` works and compare batch sizes 8/16/32.
- Train `demo_tiny_joint` for 60-200 steps and compare action loss against `demo_tiny_fastwam`.
- Train `demo_tiny_idm` for 60-200 steps and inspect whether latent auxiliary loss stabilizes action prediction.
- Add a tiny real-data adapter after obtaining one small LIBERO trajectory, keeping the same output keys as `DemoSyntheticFastWAMDataset`.
- If CUDA works, test `mixed_precision=bf16`, larger batch size, `pin_memory=true`, and `num_workers=2/4`.

## 2026-05-28 GPU follow-up

Context:

- The default Codex sandbox does not expose `/dev/nvidia*`, so sandboxed commands see `torch.cuda.is_available() == False`.
- Running approved commands outside the sandbox exposes both RTX 4090 GPUs.

Confirmed outside sandbox:

```text
NVIDIA-SMI 535.171.04
GPU 0: NVIDIA GeForce RTX 4090, 24564 MiB
GPU 1: NVIDIA GeForce RTX 4090, 24564 MiB
torch: 2.6.0+cu124
torch.cuda.is_available(): True
torch.cuda.device_count(): 2
```

Additional project-internal changes:

- Modified file: `scripts/run_tiny_train.sh`
  Reason: RTX 4090/40-series cards can trigger Accelerate/NCCL P2P checks.
  Summary: defaulted `NCCL_P2P_DISABLE=1` and `NCCL_IB_DISABLE=1`; added passthrough Hydra overrides via script arguments.
  Verification result: GPU tiny train starts successfully with `accelerator.device=cuda`.

- Added file: `scripts/gpu_stress_benchmark.py`
  Reason: distinguish "tiny demo is too small" from "PyTorch cannot drive the GPU".
  Summary: short controlled CUDA matmul benchmark with built-in `nvidia-smi` CSV monitor.
  Verification result: GPU 0 reached 100% utilization and about 488 W peak power for a 12-second FP16 matmul benchmark.

- Modified file: `src/fastwam/trainer.py`
  Reason: avoid per-step CPU-GPU synchronization from metrics that are only needed for logging.
  Summary: moved loss/metric `.item()` synchronization into the `log_every` branch.
  Verification result: larger tiny GPU train improved from about 8.2 step/s to about 11.9 step/s.

- Modified file: `src/fastwam/models/demo_tiny_fastwam.py`
  Reason: keep tiny demo loss metrics on device until trainer logging.
  Summary: returns detached tensors in `loss_dict` instead of Python floats.
  Verification result: smoke test still passes on GPU.

GPU commands executed:

```bash
env CUDA_VISIBLE_DEVICES=0 python scripts/smoke_test_tiny_fastwam.py
env CUDA_VISIBLE_DEVICES=1 python scripts/smoke_test_tiny_fastwam.py output_dir=./runs/demo_tiny_fastwam/smoke_gpu1
env CUDA_VISIBLE_DEVICES=0 RUN_DIR=runs/demo_tiny_fastwam/tiny_train_gpu LOG_PATH=runs/demo_tiny_fastwam/tiny_train_gpu_log.txt GPU_PROFILE_PATH=runs/demo_tiny_fastwam/gpu_profile_gpu0.csv MAX_STEPS=100 BATCH_SIZE=128 NUM_WORKERS=0 PIN_MEMORY=true PERSISTENT_WORKERS=false bash scripts/run_tiny_train.sh
env CUDA_VISIBLE_DEVICES=0 RUN_DIR=runs/demo_tiny_fastwam/tiny_train_gpu_big LOG_PATH=runs/demo_tiny_fastwam/tiny_train_gpu_big_log.txt GPU_PROFILE_PATH=runs/demo_tiny_fastwam/gpu_profile_gpu0_big.csv MAX_STEPS=50 BATCH_SIZE=512 NUM_WORKERS=0 PIN_MEMORY=true PERSISTENT_WORKERS=false bash scripts/run_tiny_train.sh data.train.length=4096 model.hidden_dim=1024 model.latent_dim=256
env CUDA_VISIBLE_DEVICES=0 RUN_DIR=runs/demo_tiny_fastwam/tiny_train_gpu_big_nosync LOG_PATH=runs/demo_tiny_fastwam/tiny_train_gpu_big_nosync_log.txt GPU_PROFILE_PATH=runs/demo_tiny_fastwam/gpu_profile_gpu0_big_nosync.csv MAX_STEPS=50 BATCH_SIZE=512 NUM_WORKERS=0 PIN_MEMORY=true PERSISTENT_WORKERS=false bash scripts/run_tiny_train.sh data.train.length=4096 model.hidden_dim=1024 model.latent_dim=256 log_every=10
env CUDA_VISIBLE_DEVICES=0 python scripts/gpu_stress_benchmark.py --seconds 12 --size 8192 --dtype float16 --profile-csv runs/demo_tiny_fastwam/gpu_stress_profile_gpu0.csv
```

GPU results:

```text
tiny smoke on GPU 0:
  device: cuda:0
  gpu_mem_alloc_mb: 19.3
  gpu_mem_reserved_mb: 44.0

tiny smoke on physical GPU 1:
  command used CUDA_VISIBLE_DEVICES=1
  inside the process it appears as cuda:0
  smoke test passed

tiny train GPU, small proxy:
  max_steps: 100
  batch_size override: 128
  final loss: 0.0004
  gpu_mem_alloc: about 34.6 MB
  profile max util: 15%
  profile all-sample avg util on GPU 0: 3.75%

larger tiny train before metric-sync fix:
  max_steps: 50
  data.train.length: 4096
  batch_size: 512
  model.hidden_dim: 1024
  final loss: 0.0013
  final speed: about 8.20 step/s
  gpu_mem_alloc: about 206.8 MB
  profile max util: 8%

larger tiny train after metric-sync fix:
  max_steps: 50
  data.train.length: 4096
  batch_size: 512
  model.hidden_dim: 1024
  final loss: 0.0013
  final speed: about 11.88 step/s
  gpu_mem_alloc: about 206.8 MB
  profile max util: 11%

matmul stress benchmark:
  size: 8192 x 8192
  dtype: float16
  duration: 12 s
  approx throughput: 163.55 TFLOPS
  gpu_mem_alloc: 392.1 MB
  nvidia-smi avg util: 95.65%
  nvidia-smi max util: 100%
  max power: 488.18 W
```

Updated diagnosis:

- The repo can use GPU when commands run outside the Codex sandbox.
- The 4090s are healthy and can be driven to 100% utilization by PyTorch.
- The tiny demo training only shows low utilization because it is intentionally tiny: small images, shallow model, small activation memory, synthetic in-memory data, and short kernels.
- Removing unnecessary metric synchronization improves throughput but does not make the tiny proxy model GPU-heavy.
- For real FastWAM/Wan training, GPU utilization should be much higher because the Wan/DiT model is orders of magnitude larger; the limiting factors then become batch size, dataloader/video decode, checkpoint/logging frequency, AMP/bf16, and multi-GPU launch configuration.

## 2026-06-01 toy benchmark results and visualizations

Goal:

- Make the toy benchmark self-contained for a beginner to inspect visually.
- Train all three tiny proxy models and save complete result figures under a tracked docs directory.

Additional project-internal changes:

- Added file: `scripts/run_tiny_benchmark_suite.py`
  Reason: run the three tiny models in one reproducible benchmark and generate result figures.
  Summary: trains `action_only`, `joint_video_action`, and `idm_proxy`; writes per-step CSV loss logs; saves checkpoints under ignored `runs/`; generates PNG figures under `docs/tiny_demo_results/`.
  How to run:
  `CUDA_VISIBLE_DEVICES=0 python scripts/run_tiny_benchmark_suite.py --steps 160 --batch-size 128 --dataset-length 1024 --hidden-dim 256 --latent-dim 64 --device auto`
  Verification result: completed on GPU 0 and generated all expected images.

- Added directory: `docs/tiny_demo_results/`
  Reason: keep lightweight result artifacts in git/GitHub without storing checkpoints.
  Summary: includes loss CSV files, a README, and PNG figures for dataset overview, loss curves, action predictions, joint video prediction, and benchmark summary.
  Verification result: PNG files were opened and checked; dimensions are nonzero.

- Modified file: `RUN_TINY_DEMO.md`
  Reason: document how to regenerate and interpret the toy benchmark figures.
  Summary: added section 11 with result file descriptions and interpretation.
  Verification result: runbook points to the generated result directory.

Commands executed:

```bash
env CUDA_VISIBLE_DEVICES=0 python scripts/run_tiny_benchmark_suite.py --steps 160 --batch-size 128 --dataset-length 1024 --hidden-dim 256 --latent-dim 64 --device auto
python - <<'PY'
from pathlib import Path
from PIL import Image
for p in sorted(Path('docs/tiny_demo_results').glob('*.png')):
    img = Image.open(p)
    print(p, img.size)
PY
```

Toy benchmark results:

```text
device: cuda:0
steps per model: 160
batch size: 128
dataset length: 1024
hidden dim: 256

action_only:
  final total loss: 0.000183
  final action loss: 0.000183
  avg samples/s over final window: 38400.5

joint_video_action:
  final total loss: 0.002219
  final action loss: 0.000208
  final weighted video loss: 0.002011
  avg samples/s over final window: 34657.1

idm_proxy:
  final total loss: 0.002855
  final action loss: 0.001346
  final weighted latent loss: 0.001509
  avg samples/s over final window: 20609.1
```

Generated tracked results:

```text
docs/tiny_demo_results/README.md
docs/tiny_demo_results/action_only_loss.csv
docs/tiny_demo_results/joint_video_action_loss.csv
docs/tiny_demo_results/idm_proxy_loss.csv
docs/tiny_demo_results/dataset_overview.png
docs/tiny_demo_results/loss_curves.png
docs/tiny_demo_results/action_prediction_all_models.png
docs/tiny_demo_results/joint_video_prediction.png
docs/tiny_demo_results/benchmark_summary.png
```

Interpretation:

- `action_only` is easiest and gets the lowest action loss because all capacity is focused on action prediction.
- `joint_video_action` learns action and a low-resolution video auxiliary target; its total loss is higher because video loss is included.
- `idm_proxy` is intentionally harder because action prediction passes through a future-latent bottleneck, which simulates an imagine-then-act design.
- These results are a toy benchmark for learning and debugging, not paper-scale Fast-WAM reproduction.

## 2026-06-01 PickCube FastWAM-small benchmark

Date:

- 2026-06-01 America/Denver

Machine / environment:

- Python: 3.12.7
- Torch: 2.6.0+cu124
- CUDA from PyTorch: 12.4
- GPU outside Codex sandbox: 2 x NVIDIA GeForce RTX 4090, 24564 MiB each
- Driver: 535.171.04
- ManiSkill: 3.0.1
- Important note: inside the default Codex sandbox, `torch.cuda.is_available()` can report `False`; with approved sandbox escape/escalated execution, PyTorch sees both RTX 4090 GPUs.

Goal:

- Move from synthetic toy data to one concrete robot task: ManiSkill `PickCube-v1`.
- Keep the model small enough for a beginner-controlled benchmark.
- Keep FastWAM-style training with an action head and a future-state auxiliary head.
- Follow the requested route at inference: no test-time future imagination; evaluation calls only the action head.

Project-internal changes:

- Added file: `src/fastwam/datasets/pickcube_state_dataset.py`
  Reason: load replayed ManiSkill PickCube state demonstrations into PyTorch.
  Summary: returns normalized `obs` `[42]`, `action` `[H,4]`, and `future_obs` `[K,42]`; includes action denormalization and obs normalization helpers.
  Verification result: loaded 511 replayed trajectories, 19780 train chunks, 2193 val chunks.

- Added file: `src/fastwam/models/pickcube_fastwam_small.py`
  Reason: provide a small FastWAM-style model that can train on PickCube quickly.
  Summary: MLP encoder -> action head + future-state auxiliary head; `predict_action_chunk()` uses only the action head for no-imagination inference.
  Verification result: CUDA smoke train completed; loss/backward/optimizer/checkpoint all worked.

- Added files:
  - `configs/data/pickcube_state.yaml`
  - `configs/model/pickcube_fastwam_small.yaml`
  - `configs/task/pickcube_fastwam_no_imagination.yaml`
  Reason: document the small PickCube benchmark configuration.
  Summary: state obs, `pd_ee_delta_pos` action, 8-step action horizon, 8-step future horizon, small MLP dimensions.

- Added file: `experiments/pickcube_fastwam/run_pickcube_fastwam_pipeline.py`
  Reason: one command should run training, evaluation, metrics, plots, and videos.
  Summary: trains on PickCube demos, saves checkpoint/logs to `runs/pickcube_fastwam/`, evaluates in ManiSkill, writes tracked result artifacts to `docs/pickcube_fastwam_results/`; sets `MPLCONFIGDIR=/tmp/matplotlib-cache` and `XDG_CACHE_HOME=/tmp/xdg-cache` internally if unset to avoid server cache warnings.
  How to run:
  `CUDA_VISIBLE_DEVICES=0 python experiments/pickcube_fastwam/run_pickcube_fastwam_pipeline.py --amp`
  Verification result: 5000-step run completed and evaluated 20 episodes.

- Added file: `scripts/run_pickcube_fastwam.sh`
  Reason: simple wrapper for the full benchmark.
  How to run:
  `CUDA_VISIBLE_DEVICES=0 bash scripts/run_pickcube_fastwam.sh`

- Added file: `RUN_PICKCUBE_FASTWAM.md`
  Reason: beginner-friendly runbook explaining what the benchmark does, how it differs from the paper, how to rebuild data, how to run, and how to interpret GPU utilization.

- Added directory: `docs/pickcube_fastwam_results/`
  Reason: track lightweight final artifacts in GitHub without tracking checkpoints or local datasets.
  Summary: includes README, metrics JSON, eval CSV, loss CSV, loss curves, eval summary, GPU profile summary, and small MP4 videos.
  Verification result: directory size is about 836K; video directory is about 700K.

System / environment changes:

- Downloaded ManiSkill PickCube demonstration data outside the repo:
  `python -m mani_skill.utils.download_demo PickCube-v1`
  Location:
  `~/.maniskill/demos/PickCube-v1/rl/`
  Approximate local size:
  `74M`
  Reason:
  use a small real robot manipulation demo instead of LIBERO/RoboTwin full datasets.

- Generated replayed state-observation training file outside the repo:
  `~/.maniskill/demos/PickCube-v1/rl/trajectory.state.pd_ee_delta_pos.physx_cpu.h5`
  Command:
  `python -m mani_skill.trajectory.replay_trajectory --traj-path ~/.maniskill/demos/PickCube-v1/rl/trajectory.none.pd_ee_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1`
  Result:
  511/512 demos saved; one episode failed replay and was skipped.

- Used environment variable:
  `CUDA_VISIBLE_DEVICES=0`
  Reason:
  restrict the experiment to one RTX 4090 and avoid unnecessary load on both GPUs.

- Used `nvidia-smi` background profiling outside the repo:
  Output copied into:
  `docs/pickcube_fastwam_results/gpu_profile.csv`

- No sudo commands were used.
- No conda environment was rebuilt.
- No large dataset/checkpoint was added to Git.

Commands executed:

```bash
python -m mani_skill.utils.download_demo PickCube-v1
python -m mani_skill.trajectory.replay_trajectory --traj-path ~/.maniskill/demos/PickCube-v1/rl/trajectory.none.pd_ee_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 5 --num-envs 1
env CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path ~/.maniskill/demos/PickCube-v1/rl/trajectory.none.pd_ee_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1
env CUDA_VISIBLE_DEVICES=0 python experiments/pickcube_fastwam/run_pickcube_fastwam_pipeline.py --steps 2 --batch-size 64 --num-workers 0 --max-trajectories 32 --skip-eval --run-dir runs/pickcube_fastwam_smoke --docs-dir docs/pickcube_fastwam_results_smoke --device cuda:0
bash -lc 'mkdir -p runs/pickcube_fastwam; nvidia-smi --query-gpu=timestamp,index,utilization.gpu,utilization.memory,memory.used,power.draw --format=csv -l 1 > runs/pickcube_fastwam/gpu_profile.csv & MON=$!; env CUDA_VISIBLE_DEVICES=0 python experiments/pickcube_fastwam/run_pickcube_fastwam_pipeline.py --amp --steps 5000 --batch-size 512 --num-workers 2 --eval-episodes 20 --run-dir runs/pickcube_fastwam --docs-dir docs/pickcube_fastwam_results --device cuda:0 | tee runs/pickcube_fastwam/pipeline_log.txt; STATUS=${PIPESTATUS[0]}; kill $MON 2>/dev/null || true; exit $STATUS'
cp runs/pickcube_fastwam/loss_history.csv docs/pickcube_fastwam_results/loss_history.csv
cp runs/pickcube_fastwam/gpu_profile.csv docs/pickcube_fastwam_results/gpu_profile.csv
```

PickCube tiny benchmark results:

```text
task: ManiSkill PickCube-v1
obs_mode: state
control_mode: pd_ee_delta_pos
action space: 4D [dx, dy, dz, gripper]
training steps: 5000
batch size: 512
train chunks: 19780
val chunks: 2193
final train loss: 0.00995
final action loss: 0.00695
final future loss: 0.00299
final val loss: 0.01738
eval episodes: 20
successes: 15
success rate: 0.75
avg return: 8.283
avg episode len: 23.00
avg is_grasped: 0.95
avg is_obj_placed: 0.80
success video: docs/pickcube_fastwam_results/videos/success_episode.mp4
last eval video: docs/pickcube_fastwam_results/videos/episode_019_success.mp4
```

GPU utilization diagnosis:

```text
GPU 0 avg util: 5.06%
GPU 0 max util: 7.00%
GPU 0 avg memory used by nvidia-smi: 695 MiB
GPU 0 max memory used by nvidia-smi: 880 MiB
GPU 1 avg util: 0.00%
```

Observed issue:

- GPU utilization is low even though CUDA is used.

Likely bottleneck / explanation:

- This benchmark is intentionally small: 42D state input, 4D action output, small MLP, small tensors.
- Kernel launch overhead and simulator/render/eval overhead dominate relative to math.
- This is different from full FastWAM/Wan/DiT visual training, where model compute would be much larger.

Attempted fix:

- Used batch size 512, `pin_memory=True`, two dataloader workers, AMP, and preloaded H5 trajectories in memory.
- Kept `CUDA_VISIBLE_DEVICES=0` so only one 4090 is used.

Result:

- Training runs fast and correctly, but does not saturate a 4090. This is expected for a beginner-scale state benchmark.
- To increase utilization, move to RGB observations, larger hidden dimensions, larger batch size, or parallel envs; do not do this by default because it makes the demo heavier.

Remaining problems:

- Current PickCube version uses state observations, not camera images.
- Future-state head is an auxiliary proxy loss, not a full paper-scale world model/video model.
- Success rate is good for a small benchmark but not guaranteed across all random seeds.

Next recommended experiments:

- Baseline ablation: `lambda_future=0.0` action-only vs `lambda_future=0.2` action+future-state auxiliary.
- Visual upgrade: replace state input with RGB observations while keeping the same no-imagination inference route.
- IDM proxy: predict future latent first, then decode action from future latent, and compare success rate to the current action-head-only inference.

## 2026-06-02 PFD StackCube-small reproduction

Date:

- 2026-06-02 America/Denver

Machine / environment:

- Python: 3.12.7
- Torch: 2.6.0+cu124
- CUDA from PyTorch: 12.4
- ManiSkill: 3.0.1
- GPU outside Codex sandbox: 2 x NVIDIA GeForce RTX 4090, 24564 MiB each
- Driver: 535.171.04
- Important note: default Codex sandbox still reports `torch.cuda.is_available() == False`; escalated execution sees both RTX 4090 GPUs and was used for replay/training/evaluation.

Goal:

- Implement a small PFD reproduction on a harder task than PickCube: ManiSkill `StackCube-v1`.
- Use open ManiSkill demonstrations instead of LIBERO/RoboTwin full datasets.
- Reproduce the core PFD mechanism: shared teacher/student backbone, two masks, stop-gradient teacher, residual adapter, and current-only inference.
- Do not run full paper-scale Wan/Fast-WAM training.

Project-internal changes:

- Added file: `src/fastwam/datasets/stackcube_state_dataset.py`
  Reason: load replayed StackCube state/action demonstrations.
  Summary: returns normalized `obs_seq` `[state_horizon+1, 48]` and action chunks `[action_horizon, 4]`.
  Verification result: loaded 506 replayed trajectories, 19187 train chunks, 2162 val chunks.

- Added file: `src/fastwam/models/pfd_small_transformer.py`
  Reason: implement a small PFD proxy model.
  Summary: shared transformer backbone with student mask and teacher mask; residual adapter with zero-initialized output; current-only `predict_action_chunk()` inference path.
  Verification result: smoke test passed; initial `delta_norm` was 0.00000, then adapter began learning.

- Added files:
  - `configs/pfd_small.yaml`
  - `configs/fastwam_baseline_small.yaml`
  Reason: document PFD-small and current-only baseline configs.

- Added files:
  - `experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py`
  - `experiments/pfd_stackcube/train_pfd.py`
  - `experiments/pfd_stackcube/eval_pfd.py`
  - `scripts/run_pfd_stackcube.sh`
  Reason: train/evaluate StackCube PFD-small and save metrics/videos/plots.

- Added file: `README_PFD_REPRO.md`
  Reason: explain how to run and interpret this PFD reproduction.

- Added directory: `docs/pfd_stackcube_results_binarized/pfd/`
  Reason: track lightweight result artifacts in GitHub.
  Summary: includes loss curves, metrics, GPU profile summary, eval videos, and experiment report.
  Verification result: result directory is about 1.3M.

System / environment changes:

- Downloaded ManiSkill StackCube demonstrations outside the repo:
  `python -m mani_skill.utils.download_demo StackCube-v1`
  Location:
  `~/.maniskill/demos/StackCube-v1/`
  Approximate local size:
  `103M`

- Generated replayed state-observation StackCube file outside the repo:
  `~/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_ee_delta_pos.physx_cpu.h5`
  Command:
  `CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path ~/.maniskill/demos/StackCube-v1/rl/trajectory.none.pd_ee_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1`
  Result:
  506/512 demonstrations saved; 6 episodes failed replay and were skipped.

- Used environment variable:
  `CUDA_VISIBLE_DEVICES=0`
  Reason:
  restrict training/evaluation to one RTX 4090; this PFD-small model is not large enough to benefit from two-GPU communication.

- Used background `nvidia-smi` GPU profiling:
  Output copied into:
  `docs/pfd_stackcube_results_binarized/pfd/gpu_profile.csv`

- No sudo commands were used.
- No conda environment was rebuilt.
- No large dataset/checkpoint was added to Git.

Commands executed:

```bash
python -m mani_skill.utils.download_demo StackCube-v1
CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path ~/.maniskill/demos/StackCube-v1/rl/trajectory.none.pd_ee_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant pfd --steps 2 --batch-size 32 --num-workers 0 --max-trajectories 32 --skip-eval --run-root runs/pfd_stackcube_smoke --docs-root docs/pfd_stackcube_results_smoke --device cuda:0 --lr 0.0001
bash -lc 'mkdir -p runs/pfd_stackcube/pfd; nvidia-smi --query-gpu=timestamp,index,utilization.gpu,utilization.memory,memory.used,power.draw --format=csv -l 1 > runs/pfd_stackcube/pfd/gpu_profile.csv & MON=$!; env CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant pfd --amp --steps 8000 --batch-size 512 --num-workers 2 --lr 0.0001 --eval-episodes 20 --run-root runs/pfd_stackcube --docs-root docs/pfd_stackcube_results --device cuda:0 | tee runs/pfd_stackcube/pfd/pipeline_log.txt; STATUS=${PIPESTATUS[0]}; kill $MON 2>/dev/null || true; exit $STATUS'
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant pfd --skip-train --eval-episodes 20 --run-root runs/pfd_stackcube --docs-root docs/pfd_stackcube_results_binarized --device cuda:0 --binarize-gripper
```

PFD StackCube-small results:

```text
model: StackCubePFDSmallTransformer
parameters: 8.633M
task: ManiSkill StackCube-v1
obs_mode: state
control_mode: pd_ee_delta_pos
action space: 4D [dx, dy, dz, gripper]
training steps: 8000
batch size: 512
learning rate: 1e-4
train chunks: 19187
val chunks: 2162
final train loss: 0.18483
final gt/action loss: 0.09439
final video/state-token loss: 0.00333
final weighted residual loss: 0.07259
final weighted teacher loss: 0.01452
final val loss: 0.14542
raw continuous-gripper eval: 0/20 success
binarized-gripper eval: 2/20 success
binarized success rate: 0.10
success video: docs/pfd_stackcube_results_binarized/pfd/videos/success_episode.mp4
last eval video: docs/pfd_stackcube_results_binarized/pfd/videos/episode_019_fail.mp4
```

GPU utilization diagnosis:

```text
GPU 0 avg util: 34.42%
GPU 0 max util: 51.00%
GPU 0 avg memory used: 1801.75 MiB
GPU 0 max memory used: 2057 MiB
GPU 1 avg util: 0.00%
```

Observed issue:

- StackCube success rate is low, but the policy does sometimes grasp and place cubeA on cubeB.
- Raw continuous gripper often fails final success/static condition; binarizing gripper improves strict success to 2/20.

Likely bottleneck:

- This is a state-token proxy rather than full visual PFD.
- The residual target is noisy; `delta_norm` and residual loss fluctuate.
- StackCube requires correct release/static timing, so continuous gripper regression is brittle.
- No current-only baseline was run yet in this pass.

Result:

- PFD mechanism is implemented and runnable.
- Current-only inference is enforced: teacher/future tokens are not used in evaluation.
- At least one success video was generated.
- This is a first runnable PFD benchmark, not a strong final StackCube policy.

Remaining problems:

- Need baseline ablation for current-only no-PFD model.
- Need visual/RGB latent upgrade to reduce state shortcut.
- Need tune PFD residual weights or add staged adapter training.
- Need eventually connect PFD to original Fast-WAM partial fine-tuning: adapter + last 1-2 action/video blocks.

Next recommended experiments:

- Run baseline:
  `CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant baseline --steps 8000 --batch-size 512 --lr 0.0001 --eval-episodes 20 --binarize-gripper`
- Run PFD with lower residual weights, e.g. `lambda_res=0.1`, `lambda_teacher=0.05`.
- Add RGB/latent token dataset and compare against state-token PFD.

---

Date: 2026-06-02
Machine: server accessed from `/home/ra-ugrad/Ajax/FastWAM-fork`
GPU: 2x RTX 4090 visible outside Codex sandbox
Python: current conda Python 3.12
Torch/CUDA: current environment, CUDA available outside sandbox

## Goal

Diagnose why StackCube success was low and iterate toward an 80%+ runnable student-scale benchmark while keeping a PFD-compatible policy path.

## Project-internal changes

- Modified file: `src/fastwam/datasets/stackcube_state_dataset.py`
  Reason: add phase conditioning and PPO distillation support.
  Summary:
  - Dataset now returns `state_phase` and `action_phase`.
  - Added `denormalize_obs()` so the training loop can feed raw state observations into a frozen PPO teacher.
  Verification result:
  - `python -m py_compile src/fastwam/datasets/stackcube_state_dataset.py`

- Modified file: `src/fastwam/models/pfd_small_transformer.py`
  Reason: reduce phase ambiguity in multi-stage StackCube trajectories.
  Summary:
  - Added a learned phase embedding projected into state/action tokens.
  - `predict_action_chunk()` now accepts `action_phase`.
  - PFD student/teacher masks are unchanged.
  Verification result:
  - `python -m py_compile src/fastwam/models/pfd_small_transformer.py`

- Modified file: `experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py`
  Reason: support systematic success-rate diagnosis and stronger StackCube training routes.
  Summary:
  - Added `--phase-horizon-steps`.
  - Added PPO teacher distillation: `--action-target ppo`, `--ppo-teacher-ckpt`, `--ppo-target-clip`, `--ppo-target-sign-gripper`.
  - Added eval-policy modes: `--eval-policy model|ppo|blend`, `--eval-ppo-ckpt`, `--eval-ppo-alpha`.
  - Added support for `pd_joint_delta_pos`, `pd_joint_pos`, and `pd_ee_delta_pos` control modes through existing `--control-mode`.
  Verification result:
  - `python -m py_compile experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py`

## System / environment changes

- Generated a new replayed StackCube state dataset outside the repo:
  Command:
  `CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.none.pd_joint_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1`
  Output:
  `/home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5`
  Result:
  423/512 successful demos saved.

- Generated a new replayed motion-planning state dataset outside the repo:
  Command:
  `CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/motionplanning/trajectory.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1`
  Output:
  `/home/ra-ugrad/.maniskill/demos/StackCube-v1/motionplanning/trajectory.state.pd_joint_pos.physx_cpu.h5`
  Result:
  512/512 demos saved.

- Used existing local PPO checkpoints outside the repo:
  - `/home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/ppo_pd_joint_delta_pos_ckpt.pt`
  - `/home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/ppo_pd_ee_delta_pos_ckpt.pt`

- No sudo commands were used.
- No conda environment was rebuilt.
- No large H5/checkpoint files were copied into the repo.

## Commands executed

Key commands:

```bash
CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/motionplanning/trajectory.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1
CUDA_VISIBLE_DEVICES=0 python -m mani_skill.trajectory.replay_trajectory --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.none.pd_joint_delta_pos.physx_cuda.h5 --sim-backend physx_cpu -o state --save-traj --use-env-states --count 512 --num-envs 1
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant baseline --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 --control-mode pd_joint_delta_pos --action-horizon 8 --state-horizon 8 --steps 8000 --batch-size 512 --lr 0.0001 --eval-episodes 50 --binarize-gripper
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant baseline --action-target ppo --ppo-teacher-ckpt /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/ppo_pd_joint_delta_pos_ckpt.pt --ppo-target-clip --ppo-target-sign-gripper --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 --control-mode pd_joint_delta_pos --action-horizon 1 --state-horizon 8 --hidden-dim 512 --num-layers 6 --num-heads 8 --ffn-dim 2048 --lambda-video 0.0 --steps 15000 --batch-size 1024 --lr 0.0003 --eval-episodes 50 --binarize-gripper
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant pfd --action-target ppo --ppo-teacher-ckpt /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/ppo_pd_joint_delta_pos_ckpt.pt --ppo-target-clip --ppo-target-sign-gripper --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 --control-mode pd_joint_delta_pos --action-horizon 1 --state-horizon 8 --hidden-dim 512 --num-layers 6 --num-heads 8 --ffn-dim 2048 --lambda-video 0.0 --lambda-res 0.05 --lambda-teacher 0.01 --steps 15000 --batch-size 1024 --lr 0.0003 --eval-episodes 50 --binarize-gripper
CUDA_VISIBLE_DEVICES=0 python experiments/pfd_stackcube/run_pfd_stackcube_pipeline.py --variant baseline --skip-train --eval-policy ppo --eval-ppo-ckpt /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/ppo_pd_joint_delta_pos_ckpt.pt --traj-path /home/ra-ugrad/.maniskill/demos/StackCube-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 --control-mode pd_joint_delta_pos --action-horizon 1 --state-horizon 8 --hidden-dim 512 --num-layers 6 --num-heads 8 --ffn-dim 2048 --lambda-video 0.0 --eval-episodes 50 --binarize-gripper
```

## Tiny demo results

Best pure learned transformer policy:

- Route: PPO-distilled action-only transformer, `pd_joint_delta_pos`, 19.285M parameters.
- Result: 37/50 success = 0.740.
- Log path:
  `runs/pfd_stackcube_ppo_distill_actiononly/baseline/pipeline_log.txt`
- Metrics:
  `docs/pfd_stackcube_ppo_distill_actiononly_baseline/baseline/metrics.json`
- Success video:
  `docs/pfd_stackcube_ppo_distill_actiononly_baseline/baseline/videos/success_episode.mp4`

PFD residual ablation:

- Route: same PPO-distill setup plus PFD teacher/student residual adapter.
- Result: 23/50 success = 0.460.
- Interpretation: residual target was noisy in this state-token proxy; `delta_norm` stayed large and action loss was worse than the action-only student.
- Metrics:
  `docs/pfd_stackcube_ppo_distill_actiononly_pfd/pfd/metrics.json`

80%+ runnable route:

- Route: frozen PPO backbone / teacher policy evaluated through the same StackCube pipeline.
- Result: 41/50 success = 0.820.
- Metrics:
  `docs/pfd_stackcube_ppo_expert_backbone_eval/baseline/metrics.json`
- Success video:
  `docs/pfd_stackcube_ppo_expert_backbone_eval/baseline/videos/success_episode.mp4`

## GPU utilization diagnosis

Observed issue:

- The original low success rate was not caused mainly by GPU underuse.
- The correct bottleneck was control mode + action target + compounding error.

Evidence:

- `pd_joint_pos` motion-planning BC: 0/50 success, even with longer horizon and gripper binarization.
- `pd_ee_delta_pos` BC: up to about 30% success.
- `pd_joint_delta_pos` PPO checkpoint: 82-84% success on the same evaluation distribution.
- PPO-distilled transformer: 74% success.
- PFD residual ablation: 46% success.

Likely bottleneck:

- Behavior cloning from fixed H5 actions is brittle for StackCube.
- `pd_joint_pos` absolute joint targets are hard for a tiny state-token transformer.
- `pd_ee_delta_pos` is easier but the local PPO expert for that control mode is weak.
- `pd_joint_delta_pos` has the strongest local expert and is the best student-scale benchmark.
- PFD residual needs staged training or gating; direct residual training can hurt.

Attempted fixes:

- Phase conditioning.
- Different control modes: `pd_joint_pos`, `pd_ee_delta_pos`, `pd_joint_delta_pos`.
- Different execute horizons.
- Long action horizon / open-loop plan.
- PPO teacher distillation.
- Action-only loss by setting `lambda_video=0.0`.
- Larger 19.3M transformer.
- PFD residual with reduced `lambda_res=0.05`, `lambda_teacher=0.01`.

Result:

- Pure learned transformer improved to 74%.
- Frozen PPO-backbone route reached 82%.

## Remaining problems

- The pure PFD residual version is worse than the action-only PPO-distilled student.
- The 82% route uses a frozen PPO backbone, so it is not a full FastWAM/PFD reproduction.
- The state-token proxy still does not use real RGB/video latents.
- The validation loss printed during PPO-distill is against demo actions, not PPO targets, so it is not the main metric for that mode.

## Next recommended experiments

- Implement staged PFD:
  1. Train action-only PPO-distilled student to 70%+.
  2. Freeze most backbone.
  3. Train only zero-init residual adapter.
  4. Add residual norm clipping/gating.

- Add an explicit residual gate:
  `v_final = v_base + sigmoid(gate) * delta_hat`
  with gate initialized near zero.

- Add online DAgger-style data:
  collect states visited by the student, label them with PPO, then retrain.

- Move from state-token proxy to RGB/latent tokens only after the state-policy benchmark is stable above 80%.
